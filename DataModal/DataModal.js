


window.setInterval('refreshData()', 10000); 	
    // Call a function every 10000 milliseconds 
    // (OR 10 seconds).
    // Refresh or reload page.
    function refreshData() {
        //Data.CryptoObject.name = Math.random()*100
        console.log(Data)
    }